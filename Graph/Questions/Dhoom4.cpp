#include <iostream>
#include <string>
#include <unordered_set>
#include <unordered_map>
#include <vector>
#include <algorithm>
#include <queue>
#define ll long long
using namespace std;
#define mod 100000;

void dhoom(ll k1, ll k2, ll n, vector<ll> &arr)
{
    
}

int main()
{
    ll k1, k2, n;
    cin >> k1 >> k2 >> n;
    vector<ll> arr(n);
    for (ll i = 0; i < n; i++)
        cin >> arr[i];
    dhoom(k1, k2, n, arr);

    return 0;
}